import * as electron from 'electron';
import * as React from 'react';
// import {bootstrapIPC} from '../../base/parts/ipc/ipc.renderer';

console.log('1111electron', electron);
console.log('react', React);
// bootstrapIPC(app);
